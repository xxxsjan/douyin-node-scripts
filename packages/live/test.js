const pc = require("picocolors");

const fs = require("fs");
const path = require("path");
const prompts = require("prompts");
const { createCwdCacheFile } = require("utils");

(async function () {
  // const response = await prompts({
  //   type: "text",
  //   name: "name",
  //   message: "文件名",
  //   initial: "test.txt",
  // });
})();
